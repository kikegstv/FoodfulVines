rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir lectura/escritura para todos (solución temporal)
      allow read, write: if true;
    }
  }
}
